# contributing

`npm install` to set up. `npm test` to run the test suite. `npm run dev` to start the dev server.

This is a Preact + Vite PWA. All data lives in IndexedDB via Dexie. If you want to improve the food parser dictionary, edit src/food-parser.ts.
